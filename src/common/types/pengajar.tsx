export type Pengajar = {
  id: string;
  nama: string;
};

export type PengajarSelect = {
  value: string;
  label: string;
};

export type PengajarDetail = {
  id: string;
  nama: string;
  // Add more fields if needed
};
