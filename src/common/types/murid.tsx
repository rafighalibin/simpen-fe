export type MuridRead = {
  id: string;
  nama: string;
  namaOrtu: string;
  emailOrtu: string;
  noHpOrtu: string;
};

export type MuridSelect = {
  value: string;
  label: string;
};

export type MuridDetail = {
  id: string;
  nama: string;
  // TODO: Add more fields if needed
};
