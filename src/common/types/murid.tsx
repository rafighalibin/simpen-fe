export type Murid = {
  id: string;
  nama: string;
};

export type MuridSelect = {
  value: string;
  label: string;
};

export type MuridDetail = {
  id: string;
  nama: string;
  // TODO: Add more fields if needed
};
