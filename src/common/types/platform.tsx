export type ReadJadwalZoom = {
  id: string;
  platformId: string;
  nama: string;
  hostKey: string;
  link: string;
};
export type ZoomSelect = {
  value: string;
  label: string;
};

export type ReadZoom = {
  id: string;
  nama: string;
  hostKey: string;
  link: string;
};
